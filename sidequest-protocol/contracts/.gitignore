# Slither run artefacts: regenerated on every deploy, never committed.
slither-report.json
slither-run.log

# Cached address of the drill fixture; local to a chain, not shared.
.lossy-target-*

# Generated by script/seed-vaults-sql.sh; regenerate rather than commit.
seed.sql

# Addresses of the lifecycle drill fixtures; local to a chain.
.priced-adapter-*
.spot-v2-*
